import * as React from 'react';

export default function SimpleBottomNavigation() {
  const [value, setValue] = React.useState(0);

  return (
    <div className='fixed inset-x-0 bottom-0 md:hidden bar'>
    <button>
      
    </button>

    <button>
      
    </button>
  </div>
  );
}